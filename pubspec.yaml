name: clean_architecure_course
description: A new Flutter project.


publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.17.5 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
    # Service locator/dependency_injector: 
  get_it: ^7.2.0
    # Bloc for state management
  flutter_bloc: ^8.0.1
    # Value equality
  equatable: ^2.0.3
    # Functional programming thingies
  dartz: ^0.10.1
    # Remote API
  data_connection_checker: ^0.3.4
  http: ^0.13.5
    # Local cache
  shared_preferences: ^2.0.15
  #mocktail




dev_dependencies:
  flutter_test:
    sdk: flutter
  mockito: ^5.3.0
  build_runner: ^2.2.0
  mocktail: ^0.3.0



  flutter_lints: ^2.0.0


flutter:

  uses-material-design: true


